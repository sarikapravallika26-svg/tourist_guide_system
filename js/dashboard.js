// Placeholder dashboard logic for future backend integration
const dashboardStats = document.querySelectorAll('.stat-card p');
if (dashboardStats.length) {
  dashboardStats.forEach((item) => {
    item.textContent = item.textContent;
  });
}
